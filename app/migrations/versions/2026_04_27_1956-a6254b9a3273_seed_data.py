"""seed data

Revision ID: a6254b9a3273
Revises: 2a26ded24831
Create Date: 2026-04-27 19:56:11.434299

"""

from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "a6254b9a3273"
down_revision: Union[str, Sequence[str], None] = "2a26ded24831"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


# ─────────────────────────────────────────────────────────────
#  Хелпер
# ─────────────────────────────────────────────────────────────
def _update_task(title: str, description: str, starter_code: str) -> None:
    """
    Обновляет описание и стартовый код задания, найденного по `title`.
    Все 17 заголовков в проекте уникальны глобально.

    Используем `op.get_bind().exec_driver_sql`, а не `op.execute`, чтобы
    SQLAlchemy не парсила содержимое описаний — иначе подстроки вида
    `:mid`, `:i`, `:word` интерпретируются как именованные bind-параметры
    и падают с `A value is required for bind parameter 'mid'`.
    """
    op.get_bind().exec_driver_sql(f"""
        UPDATE tasks
        SET description = $desc${description}$desc$,
            starter_code = $starter${starter_code}$starter$
        WHERE title = '{title}';
    """)


# ─────────────────────────────────────────────────────────────
#  НОВЫЕ ОПИСАНИЯ И СТАРТЕРЫ (upgrade)
# ─────────────────────────────────────────────────────────────

# === NumPy ===

NEW_NUMPY_TASK1_DESC = r"""Напишите функцию `solution(arr)`, которая принимает список чисел и возвращает **сумму всех его элементов** в виде целого числа. Используйте библиотеку NumPy.

### Что принимает

- `arr` (`list[int]`) — список целых чисел.

### Что возвращает

- `int` — сумма всех элементов.

### Примеры

```python
solution([1, 2, 3, 4, 5])  # → 15
solution([0, -1, 1])       # → 0
solution([100])            # → 100
```

### Требования

- Используйте `numpy` для вычисления суммы.
- Возвращаемое значение должно иметь тип встроенного `int` (не `numpy.int64`).

### Ограничения

- Длина массива: от 1 до 10 000 элементов.
- Элементы массива: целые числа от −10 000 до 10 000.

### Подсказка

`np.sum(...)` суммирует элементы массива. Чтобы привести результат к встроенному `int`, используйте `int(...)`.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_NUMPY_TASK1_STARTER = """\
import numpy as np


def solution(arr):
    # Ваш код здесь

    return ...  # замените на int — сумму всех элементов массива
"""


NEW_NUMPY_TASK2_DESC = r"""Напишите функцию `solution(arr)`, которая принимает список чисел и возвращает **словарь** со статистическими характеристиками. Используйте библиотеку NumPy.

### Что принимает

- `arr` (`list[int | float]`) — непустой список чисел.

### Что возвращает

- `dict` со следующими ключами:
  - `"mean"` (`float`) — среднее арифметическое, округлённое до 4 знаков.
  - `"std"` (`float`) — стандартное отклонение, округлённое до 4 знаков.
  - `"min"` (`float`) — минимальное значение.
  - `"max"` (`float`) — максимальное значение.

### Примеры

```python
solution([1, 2, 3, 4, 5])
# → {"mean": 3.0, "std": 1.4142, "min": 1.0, "max": 5.0}

solution([10, 10, 10])
# → {"mean": 10.0, "std": 0.0, "min": 10.0, "max": 10.0}
```

### Требования

- Используйте `np.mean`, `np.std`, `np.min`, `np.max`.
- `np.std` по умолчанию считает **population standard deviation** (`ddof=0`) — менять этот параметр не нужно.
- Округление: `round(value, 4)`.

### Подсказка

Преобразуйте список в `np.array(arr, dtype=float)` — это упростит и сами вычисления, и приведение результатов к `float` через `float(...)`.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_NUMPY_TASK2_STARTER = """\
import numpy as np


def solution(arr):
    # Ваш код здесь

    return ...  # замените на dict с ключами "mean", "std", "min", "max"
"""


NEW_NUMPY_TASK3_DESC = r"""Напишите функцию `solution(matrix)`, которая принимает двумерный список (матрицу) и возвращает её **транспонированную версию** в виде списка списков. Используйте библиотеку NumPy.

### Что принимает

- `matrix` (`list[list[int]]`) — матрица; внутренние списки одинаковой длины.

### Что возвращает

- `list[list[int]]` — транспонированная матрица. Если исходная имеет размер m × n, результат имеет размер n × m.

### Примеры

```python
solution([[1, 2, 3], [4, 5, 6]])
# → [[1, 4], [2, 5], [3, 6]]

solution([[1, 2], [3, 4]])
# → [[1, 3], [2, 4]]

solution([[5]])
# → [[5]]
```

### Требования

- Используйте NumPy для транспонирования (атрибут `.T` или функция `np.transpose`).
- Возвращайте именно `list`, не `np.ndarray`.

### Подсказка

`np.array(matrix).T.tolist()` превращает список в массив, транспонирует его и возвращает обратно в виде списка.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_NUMPY_TASK3_STARTER = """\
import numpy as np


def solution(matrix):
    # Ваш код здесь

    return ...  # замените на list[list] — транспонированную матрицу
"""


# === Сортировки (учебная тема) ===

NEW_SORT_LEARN_TASK1_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку пузырьком** (Bubble Sort) и возвращает отсортированный по возрастанию список.

Алгоритм: на каждом проходе сравниваются соседние пары элементов и, если они стоят не по порядку, меняются местами. После каждого прохода самый большой элемент «всплывает» в конец массива.

### Что принимает

- `arr` (`list[int]`) — список целых чисел (может содержать один элемент).

### Что возвращает

- `list[int]` — отсортированный по возрастанию список.

### Примеры

```python
solution([64, 34, 25, 12, 22, 11, 90])  # → [11, 12, 22, 25, 34, 64, 90]
solution([5, 1, 4, 2, 8])               # → [1, 2, 4, 5, 8]
solution([1])                           # → [1]
```

### Требования

- Реализуйте алгоритм самостоятельно. Использовать `sorted()` и `.sort()` нельзя.

### Подсказка

Внешний цикл — `n` проходов. Внутренний — сравнение соседей `arr[j]` и `arr[j+1]` с обменом, если левое больше правого. После `i`-го прохода последние `i` элементов уже на своих местах.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_SORT_LEARN_TASK1_STARTER = """\
def solution(arr):
    # Ваш код здесь
    # Подсказка: сравнивайте соседние элементы arr[j] и arr[j+1]
    # и меняйте их местами, если arr[j] > arr[j+1]

    return ...  # замените на list — отсортированный по возрастанию список
"""


NEW_SORT_LEARN_TASK2_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку выбором** (Selection Sort) и возвращает отсортированный по возрастанию список.

Алгоритм: на каждом шаге находите индекс минимального элемента в неотсортированной части массива и меняете его местами с первым элементом этой части.

### Что принимает

- `arr` (`list[int]`) — список целых чисел.

### Что возвращает

- `list[int]` — отсортированный по возрастанию список.

### Примеры

```python
solution([64, 25, 12, 22, 11])  # → [11, 12, 22, 25, 64]
solution([3, 2, 1])             # → [1, 2, 3]
solution([1, 1, 1])             # → [1, 1, 1]
```

### Требования

- Реализуйте алгоритм самостоятельно. Использовать `sorted()` и `.sort()` нельзя.

### Подсказка

Заведите два индекса: `i` — текущая позиция, на которую вы кладёте минимум; `min_idx` — индекс наименьшего среди `arr[i:]`. После внутреннего цикла поменяйте `arr[i]` и `arr[min_idx]` местами.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_SORT_LEARN_TASK2_STARTER = """\
def solution(arr):
    # Ваш код здесь
    # Подсказка: для каждой позиции i найдите индекс минимума
    # в arr[i:] и поменяйте arr[i] с этим минимумом

    return ...  # замените на list — отсортированный по возрастанию список
"""


NEW_SORT_LEARN_TASK3_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку вставками** (Insertion Sort) и возвращает отсортированный по возрастанию список.

Алгоритм: берёте очередной элемент и «вставляете» его на правильное место среди уже отсортированных элементов слева. Аналогия — раскладывание игральных карт по руке.

### Что принимает

- `arr` (`list[int]`) — список целых чисел.

### Что возвращает

- `list[int]` — отсортированный по возрастанию список.

### Примеры

```python
solution([12, 11, 13, 5, 6])  # → [5, 6, 11, 12, 13]
solution([4, 3, 2, 1])        # → [1, 2, 3, 4]
solution([1, 2, 3])           # → [1, 2, 3]
```

### Требования

- Реализуйте алгоритм самостоятельно. Использовать `sorted()` и `.sort()` нельзя.

### Подсказка

Внешний цикл — по `i` от `1` до `len(arr) - 1`. На каждом шаге сохраните `key = arr[i]`, затем сдвигайте элементы слева вправо, пока они больше `key`. На освободившуюся позицию положите `key`.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_SORT_LEARN_TASK3_STARTER = """\
def solution(arr):
    # Ваш код здесь
    # Подсказка: для каждого i сохраните arr[i] в переменную key,
    # сдвигайте элементы вправо пока они > key,
    # потом вставьте key на освободившееся место

    return ...  # замените на list — отсортированный по возрастанию список
"""


# === Pandas ===

NEW_PANDAS_TASK1_DESC = r"""Напишите функцию `solution(data)`, которая создаёт DataFrame из переданного словаря и возвращает **список имён студентов**, набравших 80 и более баллов, отсортированный по алфавиту.

### Что принимает

- `data` (`dict`) — словарь с двумя ключами:
  - `"name"` (`list[str]`) — имена студентов.
  - `"score"` (`list[int]`) — баллы (одной длины с `"name"`).

### Что возвращает

- `list[str]` — имена студентов с `score >= 80`, отсортированные по алфавиту. Если таких нет — пустой список `[]`.

### Примеры

```python
data = {
    "name": ["Иван", "Мария", "Пётр", "Анна"],
    "score": [88, 95, 71, 80],
}
solution(data)  # → ["Анна", "Иван", "Мария"]

data = {"name": ["А", "Б"], "score": [50, 60]}
solution(data)  # → []
```

### Требования

- Используйте `pandas.DataFrame` для работы с данными.
- Сортировка — стандартная `sorted(...)` по алфавиту.

### Подсказка

`df[df["score"] >= 80]["name"]` отдаст `Series` с именами прошедших порог. Превратите её в список через `.tolist()` и отсортируйте.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_PANDAS_TASK1_STARTER = """\
import pandas as pd


def solution(data):
    # Ваш код здесь
    # 1. Создайте DataFrame из словаря data
    # 2. Отфильтруйте строки, где score >= 80
    # 3. Соберите имена в список и отсортируйте по алфавиту

    return ...  # замените на list[str] — имена, отсортированные по алфавиту
"""


NEW_PANDAS_TASK2_DESC = r"""Напишите функцию `solution(data)`, которая создаёт DataFrame и возвращает **словарь** со средней зарплатой по каждому отделу.

### Что принимает

- `data` (`dict`) — словарь с двумя ключами:
  - `"department"` (`list[str]`) — названия отделов (могут повторяться).
  - `"salary"` (`list[int | float]`) — зарплаты сотрудников (одной длины с `"department"`).

### Что возвращает

- `dict[str, float]` — отображение `"отдел" → средняя_зарплата`, округлённая до 2 знаков.

### Пример

```python
data = {
    "department": ["IT", "HR", "IT", "HR", "IT"],
    "salary": [100000, 60000, 120000, 65000, 90000],
}
solution(data)
# → {"HR": 62500.0, "IT": 103333.33}
```

### Требования

- Используйте `df.groupby("department")["salary"].mean()`.
- Округление: `round(value, 2)`.

### Подсказка

`groupby(...)["salary"].mean()` вернёт `pandas.Series`. Превратите её в словарь, например словарным включением: `{k: round(v, 2) for k, v in result.items()}`.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_PANDAS_TASK2_STARTER = """\
import pandas as pd


def solution(data):
    # Ваш код здесь
    # Подсказка: используйте df.groupby("department")["salary"].mean()
    # и округлите каждое значение до 2 знаков

    return ...  # замените на dict[str, float] — {отдел: средняя_зарплата}
"""


NEW_PANDAS_TASK3_DESC = r"""Напишите функцию `solution(data)`, которая по результатам экзаменов возвращает **имя студента с наивысшим средним баллом** по двум предметам.

### Что принимает

- `data` (`dict`) — словарь с тремя ключами:
  - `"name"` (`list[str]`) — имена студентов.
  - `"math"` (`list[int | float]`) — оценки по математике.
  - `"python"` (`list[int | float]`) — оценки по Python.
  Все три списка одинаковой длины.

### Что возвращает

- `str` — имя студента с максимальным средним баллом. Если несколько имеют одинаковое максимальное среднее — верните то, которое встречается первым.

### Пример

```python
data = {
    "name": ["Иван", "Мария", "Пётр"],
    "math": [80, 95, 70],
    "python": [90, 85, 95],
}
solution(data)  # → "Мария"  (среднее 90.0)
```

### Требования

- Используйте `pandas.DataFrame`.
- Создайте новый столбец со средним баллом и найдите строку с его максимумом.

### Подсказка

`df["average"] = (df["math"] + df["python"]) / 2`. Затем `df.loc[df["average"].idxmax(), "name"]` вернёт имя в позиции максимума.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_PANDAS_TASK3_STARTER = """\
import pandas as pd


def solution(data):
    # Ваш код здесь
    # 1. Создайте DataFrame из data
    # 2. Добавьте столбец "average" = (math + python) / 2
    # 3. Найдите имя студента с максимальным average

    return ...  # замените на str — имя студента с наивысшим средним баллом
"""


# === Хэш-таблицы (interview) ===

NEW_HASHTABLE_TASK1_DESC = r"""Дан список целых чисел `nums` и целое число `target`. Найдите **два разных** элемента в `nums`, сумма которых равна `target`, и верните **их индексы** в виде списка.

Гарантируется: ровно одно решение существует. Один и тот же элемент использовать дважды нельзя.

### Что принимает

- `nums` (`list[int]`) — список целых чисел.
- `target` (`int`) — целевая сумма.

### Что возвращает

- `list[int]` длины 2 — список из двух индексов `[i, j]`, такой что `nums[i] + nums[j] == target` и `i != j`.

### Примеры

```python
solution([2, 7, 11, 15], 9)  # → [0, 1]   (2 + 7 = 9)
solution([3, 2, 4], 6)       # → [1, 2]   (2 + 4 = 6)
solution([3, 3], 6)          # → [0, 1]
```

### Требования

- Сложность — **O(n)**, без вложенных циклов.
- Используйте хэш-таблицу (`dict`).

### Подсказка

Для каждого числа `x` нужная пара — это `target - x`. Идите по `nums` один раз, складывая в словарь уже встреченные числа вместе с их индексами; на каждом шаге проверяйте, есть ли в словаре нужное дополнение.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_HASHTABLE_TASK1_STARTER = """\
def solution(nums, target):
    # Ваш код здесь
    # Подсказка: для каждого числа x нужная пара — это (target - x).
    # Идите по списку один раз, складывая в словарь увиденные числа.

    return ...  # замените на list[int] длины 2 — индексы [i, j]
"""


NEW_HASHTABLE_TASK2_DESC = r"""Напишите функцию `solution(s)`, которая принимает строку и возвращает **первый символ, который встречается в ней ровно один раз**.

### Что принимает

- `s` (`str`) — строка (может быть пустой).

### Что возвращает

- `str` — первый уникальный символ. Если такого символа нет, верните пустую строку `""`.

### Примеры

```python
solution("leetcode")     # → "l"
solution("aabb")         # → ""
solution("loveleetcode") # → "v"
```

### Требования

- Сложность — **O(n)**, за два прохода по строке.
- Используйте словарь для подсчёта частот.

### Подсказка

Сначала за один проход посчитайте, сколько раз встречается каждый символ. Затем снова пройдитесь **по строке** (а не по словарю!) и верните первый символ с частотой 1 — порядок строк здесь важен.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_HASHTABLE_TASK2_STARTER = """\
def solution(s):
    # Ваш код здесь
    # Подсказка: посчитайте частоту каждого символа за один проход,
    # потом снова пройдитесь по строке и найдите первый с частотой 1.

    return ...  # замените на str — первый уникальный символ или ""
"""


NEW_HASHTABLE_TASK3_DESC = r"""Напишите функцию `solution(nums)`, которая принимает список целых чисел и возвращает список **уникальных дубликатов** — чисел, встречающихся в списке более одного раза.

### Что принимает

- `nums` (`list[int]`) — список целых чисел (может быть пустым).

### Что возвращает

- `list[int]` — числа, встречающиеся в `nums` более одного раза, **каждое по одному разу**. Порядок элементов не имеет значения — тесты сравнивают результат через `sorted(...)`.

### Примеры

```python
solution([4, 3, 2, 7, 8, 2, 3, 1])  # → [2, 3]   (порядок не важен)
solution([1, 1, 2])                 # → [1]
solution([1])                       # → []
```

### Требования

- Используйте словарь для подсчёта частот.

### Подсказка

Постройте словарь `{число: количество_вхождений}`, а затем верните список ключей, у которых значение больше 1. Удобно использовать списковое включение.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_HASHTABLE_TASK3_STARTER = """\
def solution(nums):
    # Ваш код здесь
    # Подсказка: посчитайте сколько раз каждое число встречается,
    # верните те, у которых счётчик > 1.

    return ...  # замените на list — числа, встречающиеся более одного раза
"""


# === Деревья (interview) ===

NEW_TREES_TASK1_DESC = r"""Дан корень бинарного дерева. Верните его **максимальную глубину** — количество узлов на самом длинном пути от корня до листа.

Класс узла дерева уже определён в стартовом коде:

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
```

### Что принимает

- `root` (`TreeNode | None`) — корень дерева. Может быть `None` (пустое дерево).

### Что возвращает

- `int` — максимальная глубина. Для пустого дерева — `0`.

### Пример

```
    3
   / \
  9  20
    /  \
   15   7
```

```python
solution(root)  # → 3
```

### Требования

- Реализуйте через рекурсию.

### Подсказка

Базовый случай: `root is None` → глубина `0`. Иначе — `1 + max(глубина_левого, глубина_правого)`. Это шаблон для большинства задач на деревья: сначала базовый случай, потом — рекурсивный шаг.

> Решение возвращается через `return` **внутри** функции `solution`. Класс `TreeNode` уже определён — изменять его не нужно.
"""

NEW_TREES_TASK1_STARTER = """\
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None


def solution(root):
    # Ваш код здесь
    # Подсказка: базовый случай — root is None → 0.
    # Иначе: 1 + max(глубина левого, глубина правого).

    return ...  # замените на int — максимальную глубину дерева
"""


NEW_TREES_TASK2_DESC = r"""Дан корень бинарного дерева. Верните его значения, обойдённые в порядке **in-order** (сначала левое поддерево, потом узел, потом правое).

Класс узла дерева уже определён в стартовом коде:

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
```

### Что принимает

- `root` (`TreeNode | None`) — корень дерева. Может быть `None`.

### Что возвращает

- `list[int]` — значения узлов в порядке in-order. Для пустого дерева — `[]`.

### Пример

```
  1
   \
    2
   /
  3
```

```python
solution(root)  # → [1, 3, 2]
```

### Требования

- Используйте рекурсию.

### Подсказка

In-order = (обход левого) + [значение текущего узла] + (обход правого). Базовый случай — пустое дерево возвращает пустой список.

> Решение возвращается через `return` **внутри** функции `solution`. Класс `TreeNode` уже определён — изменять его не нужно.
"""

NEW_TREES_TASK2_STARTER = """\
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None


def solution(root):
    # Ваш код здесь
    # Подсказка: in-order = левое поддерево + значение + правое поддерево.
    # Для пустого дерева возвращайте [].

    return ...  # замените на list — значения в порядке in-order
"""


NEW_TREES_TASK3_DESC = r"""Дан корень бинарного дерева. Верните `True`, если дерево **симметрично** относительно своей оси, и `False` иначе.

Класс узла дерева уже определён в стартовом коде:

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
```

### Что принимает

- `root` (`TreeNode | None`) — корень дерева.

### Что возвращает

- `bool` — `True`, если дерево симметрично, иначе `False`. Пустое дерево считается симметричным.

### Примеры

```
    1
   / \
  2   2       → True
 / \ / \
3  4 4  3
```

```
    1
   / \
  2   2       → False
   \   \
    3   3
```

### Требования

- Реализуйте через рекурсию.

### Подсказка

Дерево симметрично, если левое поддерево является зеркальным отражением правого. Опишите вспомогательную функцию `is_mirror(left, right)`:
- оба `None` → симметричны;
- один `None`, другой нет → нет;
- иначе значения равны и `is_mirror(left.left, right.right) and is_mirror(left.right, right.left)`.

> Решение возвращается через `return` **внутри** функции `solution`. Класс `TreeNode` уже определён — изменять его не нужно.
"""

NEW_TREES_TASK3_STARTER = """\
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None


def solution(root):
    # Ваш код здесь
    # Подсказка: дерево симметрично, если левое поддерево является
    # зеркальным отражением правого. Опишите это рекурсивно.

    return ...  # замените на bool — True если дерево симметрично
"""


# === Сортировки (interview) ===

NEW_SORT_INTERVIEW_TASK1_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку слиянием** (Merge Sort) и возвращает отсортированный по возрастанию список.

Алгоритм: разделяй и властвуй — разделите массив пополам, рекурсивно отсортируйте обе половины, затем слейте их в один отсортированный массив.

### Что принимает

- `arr` (`list[int]`) — список целых чисел.

### Что возвращает

- `list[int]` — отсортированный по возрастанию список.

### Примеры

```python
solution([5, 2, 4, 6, 1, 3])  # → [1, 2, 3, 4, 5, 6]
solution([3, 1])              # → [1, 3]
solution([1])                 # → [1]
```

### Требования

- Реализуйте алгоритм самостоятельно. Использовать `sorted()` и `.sort()` нельзя.

### Подсказка

Шаги решения:
1. Если `len(arr) <= 1` — массив уже отсортирован, верните его.
2. Найдите середину `mid = len(arr) // 2`. Рекурсивно отсортируйте `arr[:mid]` и `arr[mid:]`.
3. Слейте две отсортированные половины: возьмите два индекса `i` и `j`, идите по обеим параллельно, забирая в результат меньший из текущих элементов.

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_SORT_INTERVIEW_TASK1_STARTER = """\
def solution(arr):
    # Ваш код здесь
    # Подсказка: разделяй и властвуй —
    # 1. Если len(arr) <= 1 → вернуть arr.
    # 2. Разделить пополам, рекурсивно отсортировать каждую часть.
    # 3. Слить две отсортированные половины в один список.

    return ...  # замените на list — отсортированный по возрастанию список
"""


NEW_SORT_INTERVIEW_TASK2_DESC = r"""Напишите функцию `solution(nums, k)`, которая принимает список целых чисел и целое число `k`, и возвращает **k-й наибольший** элемент (по значению, не по индексу).

### Что принимает

- `nums` (`list[int]`) — список целых чисел длины не меньше `k`.
- `k` (`int`) — позиция от наибольшего, начиная с `1` (`k=1` — самый большой элемент).

### Что возвращает

- `int` — `k`-й по величине элемент.

### Примеры

```python
solution([3, 2, 1, 5, 6, 4], 2)            # → 5
solution([3, 2, 3, 1, 2, 4, 5, 5, 6], 4)   # → 4
solution([1], 1)                           # → 1
```

### Требования

- Можно использовать встроенную `sorted(...)`.

### Подсказка

Отсортируйте список в порядке убывания (`sorted(nums, reverse=True)`) и верните элемент с индексом `k - 1` (нумерация в Python с нуля).

> Решение возвращается через `return` **внутри** функции `solution`. Только она вызывается тестами.
"""

NEW_SORT_INTERVIEW_TASK2_STARTER = """\
def solution(nums, k):
    # Ваш код здесь
    # Подсказка: отсортируйте по убыванию и возьмите элемент с индексом k-1.

    return ...  # замените на int — k-й по величине элемент
"""


# ─────────────────────────────────────────────────────────────
#  СТАРЫЕ ОПИСАНИЯ И СТАРТЕРЫ (downgrade)
# ─────────────────────────────────────────────────────────────

OLD_NUMPY_TASK1_DESC = r"""Напишите функцию `solution(arr)`, которая принимает список чисел и возвращает **сумму всех элементов** в виде целого числа.

### Требования

- Используй `numpy` для вычисления суммы
- Функция должна возвращать значение типа `int`

### Пример

```python
solution([1, 2, 3, 4, 5])  # → 15
solution([0, -1, 1])        # → 0
solution([100])             # → 100
```

### Ограничения

- Длина массива: от 1 до 10 000 элементов
- Элементы массива: целые числа от -10 000 до 10 000
"""

OLD_NUMPY_TASK1_STARTER = """\
import numpy as np


def solution(arr):
    # Напиши своё решение здесь
    pass
"""


OLD_NUMPY_TASK2_DESC = r"""Напишите функцию `solution(arr)`, которая принимает список чисел и возвращает **словарь** со следующими статистическими характеристиками:

- `"mean"` — среднее значение (округлить до 4 знаков)
- `"std"` — стандартное отклонение (округлить до 4 знаков)
- `"min"` — минимум
- `"max"` — максимум

Используй NumPy.

### Пример

```python
solution([1, 2, 3, 4, 5])
# → {"mean": 3.0, "std": 1.4142, "min": 1.0, "max": 5.0}
```
"""

OLD_NUMPY_TASK2_STARTER = """\
import numpy as np


def solution(arr):
    # Напиши своё решение здесь
    pass
"""


OLD_NUMPY_TASK3_DESC = r"""Напишите функцию `solution(matrix)`, которая принимает двумерный список (матрицу) и возвращает **транспонированную матрицу** в виде списка списков.

Используй NumPy.

### Пример

```python
solution([[1, 2, 3], [4, 5, 6]])
# → [[1, 4], [2, 5], [3, 6]]
```
"""

OLD_NUMPY_TASK3_STARTER = """\
import numpy as np


def solution(matrix):
    # Напиши своё решение здесь
    pass
"""


OLD_SORT_LEARN_TASK1_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку пузырьком** и возвращает отсортированный по возрастанию список.

Не используй `sorted()` или `.sort()`.

### Пример

```python
solution([64, 34, 25, 12, 22, 11, 90])  # → [11, 12, 22, 25, 34, 64, 90]
solution([5, 1, 4, 2, 8])               # → [1, 2, 4, 5, 8]
solution([1])                            # → [1]
```
"""

OLD_SORT_LEARN_TASK1_STARTER = """\
def solution(arr):
    # Напиши своё решение здесь
    # Подсказка: сравнивай соседние элементы arr[j] и arr[j+1]
    # и меняй их местами, если arr[j] > arr[j+1]
    pass
"""


OLD_SORT_LEARN_TASK2_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку выбором** (Selection Sort) и возвращает отсортированный список.

Алгоритм: на каждом шаге находим индекс минимального элемента в неотсортированной части и ставим его на нужное место.

Не используй `sorted()` или `.sort()`.

### Пример

```python
solution([64, 25, 12, 22, 11])  # → [11, 12, 22, 25, 64]
solution([3, 2, 1])             # → [1, 2, 3]
```
"""

OLD_SORT_LEARN_TASK2_STARTER = """\
def solution(arr):
    # Напиши своё решение здесь
    # Подсказка: для каждой позиции i найди индекс минимума
    # в arr[i:] и поменяй arr[i] с этим минимумом
    pass
"""


OLD_SORT_LEARN_TASK3_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку вставками** (Insertion Sort) и возвращает отсортированный список.

Алгоритм: берём элемент с позиции i и «вставляем» его на правильное место среди уже отсортированных элементов слева.

Не используй `sorted()` или `.sort()`.

### Пример

```python
solution([12, 11, 13, 5, 6])  # → [5, 6, 11, 12, 13]
solution([4, 3, 2, 1])        # → [1, 2, 3, 4]
```
"""

OLD_SORT_LEARN_TASK3_STARTER = """\
def solution(arr):
    # Напиши своё решение здесь
    # Подсказка: для каждого i, сохрани arr[i] в key,
    # сдвигай элементы вправо пока они > key,
    # потом вставь key на освободившееся место
    pass
"""


OLD_PANDAS_TASK1_DESC = r"""Напишите функцию `solution(data)`, которая принимает словарь `data` с ключами `"name"` (список строк) и `"score"` (список чисел), создаёт DataFrame и возвращает **список имён студентов, набравших 80 и более баллов**, отсортированный по алфавиту.

### Пример

```python
data = {
    "name": ["Иван", "Мария", "Пётр", "Анна"],
    "score": [88, 95, 71, 80],
}
solution(data)  # → ["Анна", "Иван", "Мария"]
```
"""

OLD_PANDAS_TASK1_STARTER = """\
import pandas as pd


def solution(data):
    # Создай DataFrame из словаря data
    # Отфильтруй строки где score >= 80
    # Верни список имён, отсортированный по алфавиту
    pass
"""


OLD_PANDAS_TASK2_DESC = r"""Напишите функцию `solution(data)`, которая принимает словарь с ключами `"department"` и `"salary"`, создаёт DataFrame и возвращает **словарь** со средней зарплатой по каждому отделу (округлить до 2 знаков).

### Пример

```python
data = {
    "department": ["IT", "HR", "IT", "HR", "IT"],
    "salary": [100000, 60000, 120000, 65000, 90000],
}
solution(data)
# → {"HR": 62500.0, "IT": 103333.33}
```
"""

OLD_PANDAS_TASK2_STARTER = """\
import pandas as pd


def solution(data):
    # Создай DataFrame
    # Используй groupby("department")["salary"].mean()
    # Верни словарь {отдел: средняя_зарплата}
    pass
"""


OLD_PANDAS_TASK3_DESC = r"""Напишите функцию `solution(data)`, которая принимает словарь с ключами `"name"`, `"math"`, `"python"`, создаёт DataFrame, добавляет столбец `"average"` (среднее math и python), и возвращает имя студента с **наивысшим средним баллом**.

### Пример

```python
data = {
    "name": ["Иван", "Мария", "Пётр"],
    "math": [80, 95, 70],
    "python": [90, 85, 95],
}
solution(data)  # → "Мария"  (среднее: 90)
```
"""

OLD_PANDAS_TASK3_STARTER = """\
import pandas as pd


def solution(data):
    # Создай DataFrame
    # Добавь столбец average = (math + python) / 2
    # Верни имя студента с максимальным average
    pass
"""


OLD_HASHTABLE_TASK1_DESC = r"""Напишите функцию `solution(nums, target)`, которая принимает список целых чисел `nums` и целое число `target`, и возвращает **индексы двух чисел**, сумма которых равна `target`.

Гарантируется, что ровно одно решение существует. Нельзя использовать один элемент дважды.

**Требования:**
- Сложность O(n) — без вложенных циклов
- Используй хэш-таблицу (словарь)

### Пример

```python
solution([2, 7, 11, 15], 9)   # → [0, 1]  (2 + 7 = 9)
solution([3, 2, 4], 6)        # → [1, 2]  (2 + 4 = 6)
solution([3, 3], 6)           # → [0, 1]
```

### Подсказка

Для каждого числа `x` нужная пара — это `target - x`. Сохраняй в словаре, какие числа ты уже видел.
"""

OLD_HASHTABLE_TASK1_STARTER = """\
def solution(nums, target):
    # Напиши своё решение здесь
    pass
"""


OLD_HASHTABLE_TASK2_DESC = r"""Напишите функцию `solution(s)`, которая принимает строку `s` и возвращает **первый символ, который встречается только один раз**. Если такого нет — верните `""` (пустую строку).

**Требование:** сложность O(n).

### Пример

```python
solution("leetcode")    # → "l"
solution("aabb")        # → ""
solution("loveleetcode")# → "v"
```
"""

OLD_HASHTABLE_TASK2_STARTER = """\
def solution(s):
    # Напиши своё решение здесь
    pass
"""


OLD_HASHTABLE_TASK3_DESC = r"""Напишите функцию `solution(nums)`, которая принимает список целых чисел и возвращает список **уникальных дубликатов** — чисел, встречающихся в списке более одного раза.

Порядок в ответе не важен.

### Пример

```python
solution([4, 3, 2, 7, 8, 2, 3, 1])  # → [2, 3]
solution([1, 1, 2])                   # → [1]
solution([1])                         # → []
```
"""

OLD_HASHTABLE_TASK3_STARTER = """\
def solution(nums):
    # Напиши своё решение здесь
    pass
"""


OLD_TREES_TASK1_DESC = r"""Дан класс узла дерева:

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
```

Напишите функцию `solution(root)`, которая принимает корень бинарного дерева и возвращает **максимальную глубину** дерева.

Глубина — количество узлов вдоль самого длинного пути от корня до листового узла.

### Пример

```
    3
   / \
  9  20
    /  \
   15   7
```

```python
solution(root)  # → 3
```
"""

OLD_TREES_TASK1_STARTER = """\
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None


def solution(root):
    # Напиши своё решение здесь
    pass
"""


OLD_TREES_TASK2_DESC = r"""Дан класс узла дерева:

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
```

Напишите функцию `solution(root)`, которая возвращает **in-order обход** (левый → узел → правый) в виде списка значений.

### Пример

```
  1
   \
    2
   /
  3
```

```python
solution(root)  # → [1, 3, 2]
```
"""

OLD_TREES_TASK2_STARTER = """\
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None


def solution(root):
    # Напиши своё решение здесь
    pass
"""


OLD_TREES_TASK3_DESC = r"""Дан класс узла дерева:

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
```

Напишите функцию `solution(root)`, которая проверяет, является ли бинарное дерево **симметричным** относительно своего центра.

### Пример

```
    1
   / \
  2   2       → True (симметрично)
 / \ / \
3  4 4  3
```

```
    1
   / \
  2   2       → False
   \   \
    3   3
```
"""

OLD_TREES_TASK3_STARTER = """\
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None


def solution(root):
    # Напиши своё решение здесь
    pass
"""


OLD_SORT_INTERVIEW_TASK1_DESC = r"""Напишите функцию `solution(arr)`, которая реализует **сортировку слиянием** (Merge Sort) и возвращает отсортированный список.

### Пример

```python
solution([5, 2, 4, 6, 1, 3])  # → [1, 2, 3, 4, 5, 6]
solution([3, 1])               # → [1, 3]
solution([1])                  # → [1]
```

### Требование

Реализуй сортировку самостоятельно, без использования встроенной `sorted()`.
"""

OLD_SORT_INTERVIEW_TASK1_STARTER = """\
def solution(arr):
    # Напиши своё решение здесь
    pass
"""


OLD_SORT_INTERVIEW_TASK2_DESC = r"""Напишите функцию `solution(nums, k)`, которая принимает список целых чисел `nums` и целое `k`, и возвращает **k-й наибольший** элемент.

Это k-й наибольший по значению, а не по индексу.

### Пример

```python
solution([3, 2, 1, 5, 6, 4], 2)  # → 5
solution([3, 2, 3, 1, 2, 4, 5, 5, 6], 4)  # → 4
```

### Подсказка

Отсортируй список в порядке убывания и возьми элемент с индексом `k - 1`. Можно использовать `sorted()`.
"""

OLD_SORT_INTERVIEW_TASK2_STARTER = """\
def solution(nums, k):
    # Напиши своё решение здесь
    pass
"""


# ─────────────────────────────────────────────────────────────
#  upgrade / downgrade
# ─────────────────────────────────────────────────────────────
def upgrade() -> None:
    # NumPy
    _update_task(
        "Сумма элементов массива", NEW_NUMPY_TASK1_DESC, NEW_NUMPY_TASK1_STARTER
    )
    _update_task("Статистика массива", NEW_NUMPY_TASK2_DESC, NEW_NUMPY_TASK2_STARTER)
    _update_task(
        "Транспонирование матрицы", NEW_NUMPY_TASK3_DESC, NEW_NUMPY_TASK3_STARTER
    )

    # Сортировки (учебная тема)
    _update_task(
        "Сортировка пузырьком", NEW_SORT_LEARN_TASK1_DESC, NEW_SORT_LEARN_TASK1_STARTER
    )
    _update_task(
        "Сортировка выбором", NEW_SORT_LEARN_TASK2_DESC, NEW_SORT_LEARN_TASK2_STARTER
    )
    _update_task(
        "Сортировка вставками", NEW_SORT_LEARN_TASK3_DESC, NEW_SORT_LEARN_TASK3_STARTER
    )

    # Pandas
    _update_task(
        "Фильтрация студентов", NEW_PANDAS_TASK1_DESC, NEW_PANDAS_TASK1_STARTER
    )
    _update_task(
        "Средняя зарплата по отделам", NEW_PANDAS_TASK2_DESC, NEW_PANDAS_TASK2_STARTER
    )
    _update_task("Лучший студент", NEW_PANDAS_TASK3_DESC, NEW_PANDAS_TASK3_STARTER)

    # Хэш-таблицы (interview)
    _update_task("Two Sum", NEW_HASHTABLE_TASK1_DESC, NEW_HASHTABLE_TASK1_STARTER)
    _update_task(
        "Первый уникальный символ",
        NEW_HASHTABLE_TASK2_DESC,
        NEW_HASHTABLE_TASK2_STARTER,
    )
    _update_task(
        "Найти дубликаты", NEW_HASHTABLE_TASK3_DESC, NEW_HASHTABLE_TASK3_STARTER
    )

    # Деревья (interview)
    _update_task(
        "Максимальная глубина дерева", NEW_TREES_TASK1_DESC, NEW_TREES_TASK1_STARTER
    )
    _update_task("Обход дерева in-order", NEW_TREES_TASK2_DESC, NEW_TREES_TASK2_STARTER)
    _update_task("Симметричное дерево", NEW_TREES_TASK3_DESC, NEW_TREES_TASK3_STARTER)

    # Сортировки (interview)
    _update_task(
        "Сортировка слиянием",
        NEW_SORT_INTERVIEW_TASK1_DESC,
        NEW_SORT_INTERVIEW_TASK1_STARTER,
    )
    _update_task(
        "K-й наибольший элемент",
        NEW_SORT_INTERVIEW_TASK2_DESC,
        NEW_SORT_INTERVIEW_TASK2_STARTER,
    )


def downgrade() -> None:
    # NumPy
    _update_task(
        "Сумма элементов массива", OLD_NUMPY_TASK1_DESC, OLD_NUMPY_TASK1_STARTER
    )
    _update_task("Статистика массива", OLD_NUMPY_TASK2_DESC, OLD_NUMPY_TASK2_STARTER)
    _update_task(
        "Транспонирование матрицы", OLD_NUMPY_TASK3_DESC, OLD_NUMPY_TASK3_STARTER
    )

    # Сортировки (учебная тема)
    _update_task(
        "Сортировка пузырьком", OLD_SORT_LEARN_TASK1_DESC, OLD_SORT_LEARN_TASK1_STARTER
    )
    _update_task(
        "Сортировка выбором", OLD_SORT_LEARN_TASK2_DESC, OLD_SORT_LEARN_TASK2_STARTER
    )
    _update_task(
        "Сортировка вставками", OLD_SORT_LEARN_TASK3_DESC, OLD_SORT_LEARN_TASK3_STARTER
    )

    # Pandas
    _update_task(
        "Фильтрация студентов", OLD_PANDAS_TASK1_DESC, OLD_PANDAS_TASK1_STARTER
    )
    _update_task(
        "Средняя зарплата по отделам", OLD_PANDAS_TASK2_DESC, OLD_PANDAS_TASK2_STARTER
    )
    _update_task("Лучший студент", OLD_PANDAS_TASK3_DESC, OLD_PANDAS_TASK3_STARTER)

    # Хэш-таблицы (interview)
    _update_task("Two Sum", OLD_HASHTABLE_TASK1_DESC, OLD_HASHTABLE_TASK1_STARTER)
    _update_task(
        "Первый уникальный символ",
        OLD_HASHTABLE_TASK2_DESC,
        OLD_HASHTABLE_TASK2_STARTER,
    )
    _update_task(
        "Найти дубликаты", OLD_HASHTABLE_TASK3_DESC, OLD_HASHTABLE_TASK3_STARTER
    )

    # Деревья (interview)
    _update_task(
        "Максимальная глубина дерева", OLD_TREES_TASK1_DESC, OLD_TREES_TASK1_STARTER
    )
    _update_task("Обход дерева in-order", OLD_TREES_TASK2_DESC, OLD_TREES_TASK2_STARTER)
    _update_task("Симметричное дерево", OLD_TREES_TASK3_DESC, OLD_TREES_TASK3_STARTER)

    # Сортировки (interview)
    _update_task(
        "Сортировка слиянием",
        OLD_SORT_INTERVIEW_TASK1_DESC,
        OLD_SORT_INTERVIEW_TASK1_STARTER,
    )
    _update_task(
        "K-й наибольший элемент",
        OLD_SORT_INTERVIEW_TASK2_DESC,
        OLD_SORT_INTERVIEW_TASK2_STARTER,
    )
